const pessoa = {
  nome: "asper",
  idade: 15,
  altura: 1.70,
  estudante: true
};

const frase = `Meu nome é ${pessoa.nome}, tenho ${pessoa.idade} anos, ${pessoa.altura}m de altura e sou estudante no padre vicente ferreira cordeiro: ${pessoa.estudante}.`;

console.log(frase);